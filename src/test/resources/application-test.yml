spring:
  flyway:
    url: jdbc:postgresql://localhost:6432/testdb
    user: test
    password: test
    locations: classpath:db/migration
    baseline-on-migrate: true
  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    username: test
    password: test
    url: jdbc:postgresql://localhost:6432/testdb
  application:
    name: test